// Feature flag toggles for the Nutrify application.
// Use these flags to enable or disable features during development.

const featureFlags = {
  // Example: 'enableHydration': false,
};

export default featureFlags;