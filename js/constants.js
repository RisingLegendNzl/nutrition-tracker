// js/constants.js
// Centralized constants: localStorage keys & event names

export const LS_KEYS = Object.freeze({
  PLAN: 'NUTRIFY__PLAN'
});

export const EVENTS = Object.freeze({
  PLAN_UPDATED: 'nutrify:planUpdated'
});
