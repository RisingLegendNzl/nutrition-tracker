// Validation functions for user input and application data.

export function validateProfile(profile) {
  // In later phases this will contain actual validation logic.
  return true;
}

export default {
  validateProfile,
};